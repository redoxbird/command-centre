// GENERATED by scripts/build-grammar.ts — do not hand-edit.
// Regenerate: deno task build:grammar. Source hash: 2bfc84d97297ce7c27d320a17ac9aa8d20c6142d7fd68d4ba5ab3fc4d6ef29b4
var CCGrammarSrc = (() => {
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __export = (target, all) => {
    for (var name in all)
      __defProp(target, name, { get: all[name], enumerable: true });
  };
  var __copyProps = (to, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to, key) && key !== except)
          __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    }
    return to;
  };
  var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

  // browser.ts
  var browser_exports = {};
  __export(browser_exports, {
    CC_INPUT_ALIASES: () => CC_INPUT_ALIASES,
    CC_INPUT_TYPES: () => CC_INPUT_TYPES,
    CC_RETIRED_TYPES: () => CC_RETIRED_TYPES,
    ccAutoGenerate: () => ccAutoGenerate,
    ccInstanceKey: () => ccInstanceKey,
    exampleFor: () => exampleFor,
    lookupType: () => lookupType,
    parseToken: () => parseToken,
    parseVarInstances: () => parseVarInstances,
    parseVars: () => parseVars,
    rangeSpec: () => rangeSpec,
    renderCmd: () => renderCmd,
    renderToken: () => renderToken,
    widgetToEInput: () => widgetToEInput
  });

  // registry.ts
  var CC_INPUT_TYPES = [
    ["input.file", "file", "File path"],
    ["input.dir", "file", "Directory path"],
    ["input.files", "file", "Multiple files (space-separated)"],
    ["input.text", "text", "Free text"],
    ["input.email", "text", "Email address"],
    ["input.url", "text", "URL"],
    ["input.search", "text", "Search string"],
    ["input.uuid", "text", "UUID v4"],
    ["input.number", "number", "Numeric value"],
    ["input.port", "number", "Port number"],
    ["input.date", "date", "Calendar date"],
    ["input.color", "color", "Color swatch (hex)"],
    ["input.password", "password", "Masked secret"],
    ["input.select", "select", "Choice list", "mp4,mkv,mov"],
    ["input.radio", "radio", "Single choice", "yes,no"],
    ["input.buttongroup", "buttongroup", "Button group (single choice)", "fast,medium,slow"],
    ["input.country", "select", "Country code", "US,GB,DE,FR,ES,IN,JP,BR,CA,AU,AF,AX,AL,DZ,AS,AD,AO,AI,AQ,AG,AR,AM,AW,AT,AZ,BS,BH,BD,BB,BY,BE,BZ,BJ,BM,BT,BO,BQ,BA,BW,BV,IO,BN,BG,BF,BI,KH,CM,CV,KY,CF,TD,CL,CN,CX,CC,CO,KM,CG,CD,CK,CR,CI,HR,CU,CW,CY,CZ,DK,DJ,DM,DO,EC,EG,SV,GQ,ER,EE,SZ,ET,FK,FO,FJ,FI,GF,PF,TF,GA,GM,GE,GH,GI,GR,GL,GD,GP,GU,GT,GG,GN,GW,GY,HT,HM,VA,HN,HK,HU,IS,ID,IR,IQ,IE,IM,IL,IT,JM,JE,JO,KZ,KE,KI,KP,KR,KW,KG,LA,LV,LB,LS,LR,LY,LI,LT,LU,MO,MG,MW,MY,MV,ML,MT,MH,MQ,MR,MU,YT,MX,FM,MD,MC,MN,ME,MS,MA,MZ,MM,NA,NR,NP,NL,NC,NZ,NI,NE,NG,NU,NF,MK,MP,NO,OM,PK,PW,PS,PA,PG,PY,PE,PH,PN,PL,PT,PR,QA,RE,RO,RU,RW,BL,SH,KN,LC,MF,PM,VC,WS,SM,ST,SA,SN,RS,SC,SL,SG,SX,SK,SI,SB,SO,ZA,GS,SS,LK,SD,SR,SJ,SE,CH,SY,TW,TJ,TZ,TH,TL,TG,TK,TO,TT,TN,TR,TM,TC,TV,UG,UA,AE,UM,UY,UZ,VU,VE,VN,VG,VI,WF,EH,YE,ZM,ZW"],
    ["input.timezone", "select", "Timezone", "UTC,America/New_York,America/Chicago,America/Denver,America/Los_Angeles,America/Anchorage,Pacific/Honolulu,America/Toronto,America/Vancouver,America/Mexico_City,America/Sao_Paulo,America/Buenos_Aires,Atlantic/Azores,Europe/London,Europe/Paris,Europe/Berlin,Europe/Rome,Europe/Madrid,Europe/Amsterdam,Europe/Zurich,Europe/Stockholm,Europe/Athens,Europe/Istanbul,Europe/Moscow,Africa/Cairo,Africa/Lagos,Africa/Johannesburg,Africa/Nairobi,Asia/Dubai,Asia/Karachi,Asia/Kolkata,Asia/Dhaka,Asia/Bangkok,Asia/Singapore,Asia/Hong_Kong,Asia/Shanghai,Asia/Tokyo,Asia/Seoul,Australia/Perth,Australia/Sydney,Pacific/Auckland"],
    ["input.currency", "select", "Currency", "USD,EUR,GBP,JPY,CHF,CAD,AUD,CNY,SEK,NZD,MXN,SGD,HKD,NOK,KRW,INR,DKK,ZAR,BRL,TWD,THB,MYR,IDR,PHP,ILS,PLN,CZK,HUF,AED,SAR,TRY,RUB,UAH,RON,NGN,KES,EGP,ARS,CLP,COP,PEN"],
    ["input.language", "select", "Language", "en,es,fr,de,zh,ja,pt,ru,ar,hi,bn,it,nl,ko,tr,pl,uk,vi,th,id,ms,he,sv,da,no,fi,el,cs,ro,hu"],
    ["input.locale", "select", "Locale", "en-US,en-GB,es-ES,fr-FR,de-DE,pt-BR,zh-CN,zh-TW,ja-JP,ko-KR,ar-SA,hi-IN,it-IT,nl-NL,ru-RU,tr-TR,pl-PL,sv-SE,da-DK,uk-UA"],
    ["input.checkbox", "checkbox", "Flag", "--flag"],
    ["input.switch", "switch", "Toggle switch", "--flag"],
    ["input.range", "range", "Range slider", "0-100"],
    ["input.time", "time", "Clock time"],
    ["input.datetime", "datetime", "Date and time"],
    ["input.textarea", "textarea", "Multiline text"],
    ["input.multiselect", "multiselect", "Multiple choice", "dev,build,test"],
    ["input.license", "select", "License (SPDX)", "MIT,Apache-2.0,GPL-3.0-only,GPL-3.0-or-later,BSD-2-Clause,BSD-3-Clause,ISC,MPL-2.0,EPL-2.0,Unlicense,CC0-1.0,CC-BY-4.0,CC-BY-SA-4.0,0BSD,Zlib,Artistic-2.0,AGPL-3.0-only,LGPL-3.0-only"],
    ["input.keyvalue", "keyvalue", "Key=value pair", "KEY=value"]
  ];
  var CC_RETIRED_TYPES = [
    ["input.regex", "text", "Regular expression"],
    ["input.message", "textarea", "Short message"],
    ["input.title", "text", "Title"],
    ["input.description", "textarea", "Longer description"],
    ["input.username", "text", "User name"],
    ["input.hostname", "text", "Host name"],
    ["input.domain", "text", "Domain name"],
    ["input.ip", "text", "IP address"],
    ["input.count", "number", "Count"],
    ["input.quantity", "number", "Quantity"],
    ["input.percent", "range", "Percent slider", "0-100"],
    ["input.month", "range", "Month slider", "1-12"],
    ["input.day", "range", "Day-of-month slider", "1-31"],
    ["input.hour", "time", "Hour (clock time)"],
    ["input.minute", "time", "Minute (clock time)"],
    ["input.second", "time", "Second (clock time)"],
    ["input.bitrate", "number", "Bitrate value (k)"],
    ["input.extension", "select", "File extension", "mp4,mkv,mov,mp3,wav,avi,flv,webm"],
    ["input.container", "select", "Container format", "mp4,mkv,mov,avi,webm"],
    ["input.vcodec", "select", "Video codec", "libx264,libx265,vp9,av1,mpeg4"],
    ["input.acodec", "select", "Audio codec", "aac,mp3,opus,flac,vorbis"],
    ["input.preset", "select", "Encoder preset", "ultrafast,superfast,veryfast,faster,fast,medium,slow,slower,veryslow"],
    ["input.crf", "range", "Quality CRF (lower is better)", "0-51"],
    ["input.resolution", "select", "Resolution", "1920x1080,1280x720,3840x2160,640x480"],
    ["input.fps", "number", "Frames per second"],
    ["input.samplerate", "select", "Audio sample rate", "44100,48000,22050,96000"],
    ["input.channels", "select", "Audio channels", "1,2,6"],
    ["input.duration", "text", "Duration (e.g. 00:01:20)"],
    ["input.volume", "range", "Volume level", "0-100"],
    ["input.speed", "number", "Speed multiplier"],
    ["input.seek", "text", "Seek position (e.g. 00:00:10)"],
    ["input.subtitle", "file", "Subtitle file"],
    ["input.poster", "file", "Poster image"],
    ["input.thumbnail", "file", "Thumbnail image"],
    ["input.path", "file", "File or directory path"],
    ["input.folder", "file", "Folder path"],
    ["input.filename", "text", "File name"],
    ["input.glob", "text", "Glob pattern (e.g. *.js)"],
    ["input.pattern", "text", "Match pattern"],
    ["input.output", "file", "Output path"],
    ["input.template", "file", "Template file"],
    ["input.config", "file", "Config file"],
    ["input.logfile", "file", "Log file"],
    ["input.env", "select", "Environment", "development,staging,production"],
    ["input.script", "text", "Script name"],
    ["input.package", "text", "Package name"],
    ["input.pkgmanager", "select", "Package manager", "npm,bun,yarn,pnpm"],
    ["input.version", "text", "Version string"],
    ["input.semver", "text", "Semver (e.g. 1.2.3)"],
    ["input.branch", "text", "Git branch"],
    ["input.tag", "text", "Git tag"],
    ["input.remote", "select", "Git remote", "origin,upstream"],
    ["input.commit", "text", "Commit ref or message"],
    ["input.author", "text", "Author name"],
    ["input.logformat", "select", "Git log format", "oneline,short,medium,full"],
    ["input.repos", "text", "Repository"],
    ["input.org", "text", "Organization"],
    ["input.mode", "select", "Mode", "development,production"],
    ["input.loglevel", "select", "Log level", "error,warn,info,debug"],
    ["input.image", "select", "Container image", "nginx,httpd,caddy,node,postgres"],
    ["input.cimage", "text", "Container image (any)"],
    ["input.containername", "text", "Container name"],
    ["input.mount", "text", "Volume mapping"],
    ["input.network", "text", "Network name"],
    ["input.replicas", "number", "Replica count"],
    ["input.memory", "text", "Memory limit (e.g. 512m)"],
    ["input.cpu", "number", "CPU limit"],
    ["input.workdir", "file", "Working directory"],
    ["input.entrypoint", "text", "Entrypoint command"],
    ["input.restart", "select", "Restart policy", "no,always,on-failure,unless-stopped"],
    ["input.protocol", "select", "Protocol", "http,https,ssh,ftp"],
    ["input.method", "select", "HTTP method", "GET,POST,PUT,PATCH,DELETE"],
    ["input.header", "text", "HTTP header (Key: value)"],
    ["input.query", "text", "Query string"],
    ["input.cidr", "text", "CIDR range"],
    ["input.sql", "textarea", "SQL query"],
    ["input.mac", "text", "MAC address"],
    ["input.database", "text", "Database name"],
    ["input.table", "text", "Table name"],
    ["input.dbuser", "text", "DB user"],
    ["input.engine", "select", "DB engine", "postgres,mysql,sqlite,mongo"],
    ["input.cron", "text", "Cron expression"],
    ["input.timeout", "number", "Timeout seconds"],
    ["input.retries", "number", "Retry count"],
    ["input.delay", "number", "Delay seconds"],
    ["input.format", "select", "Output format", "json,yaml,text,table"],
    ["input.shell", "select", "Shell", "bash,sh,zsh,fish"],
    ["input.os", "select", "OS", "linux,macos,windows"],
    ["input.arch", "select", "CPU arch", "x64,arm64"],
    ["input.flag", "checkbox", "Flag (custom value via :flag)"],
    ["input.confirm", "checkbox", "Confirm toggle"],
    ["input.verbose", "checkbox", "Verbose flag", "--verbose"],
    ["input.quiet", "checkbox", "Quiet flag", "--quiet"],
    ["input.force", "checkbox", "Force flag", "--force"],
    ["input.dryrun", "checkbox", "Dry-run flag", "--dry-run"],
    ["input.colorname", "text", "Color name"],
    ["input.namespace", "text", "Namespace"],
    ["input.deployment", "text", "Deployment name"],
    ["input.service", "text", "Service name"],
    ["input.pod", "text", "Pod name"],
    ["input.context", "text", "Context name"],
    ["input.region", "select", "Cloud region", "us-east-1,us-west-2,eu-west-1,eu-central-1,ap-south-1"],
    ["input.profile", "text", "Cloud profile"],
    ["input.bucket", "text", "Bucket name"],
    ["input.bucketkey", "text", "Bucket key"],
    ["input.workspace", "text", "Workspace name"],
    ["input.varfile", "file", "Var file"],
    ["input.planfile", "file", "Plan file"]
  ];
  var CC_INPUT_ALIASES = {
    "input.hexcolor": "input.color",
    "input.bgcolor": "input.color",
    "input.fgcolor": "input.color",
    "input.token": "input.password",
    "input.apikey": "input.password",
    "input.secret": "input.password",
    "input.since": "input.date"
  };
  function lookupType(name) {
    for (const row of CC_INPUT_TYPES) {
      if (row[0] === name) return { row, retired: false };
    }
    for (const row of CC_RETIRED_TYPES) {
      if (row[0] === name) return { row, retired: true };
    }
    const target = CC_INPUT_ALIASES[name];
    if (target) {
      for (const row of CC_INPUT_TYPES) {
        if (row[0] === target) return { row, retired: false };
      }
    }
    return null;
  }
  var TEXT_FORMATS = {
    "input.email": "email",
    "input.url": "url",
    "input.search": "search",
    "input.uuid": "uuid"
  };
  var FILE_TOKENS = /* @__PURE__ */ new Set(["input.file", "input.dir", "input.files"]);
  var SELECT_FAMILY = /* @__PURE__ */ new Set([
    "input.select",
    "input.country",
    "input.timezone",
    "input.currency",
    "input.language",
    "input.locale",
    "input.license"
  ]);
  var WIDGET_TO_EINPUT = {
    text: "text",
    number: "number",
    date: "date",
    time: "date",
    datetime: "date",
    color: "color",
    password: "password",
    select: "select",
    radio: "radio",
    buttongroup: "radio",
    multiselect: "checkbox-group",
    checkbox: "checkbox",
    switch: "checkbox",
    range: "range",
    textarea: "textarea"
  };
  function widgetToEInput(token) {
    const { name, type: widget } = token;
    if (FILE_TOKENS.has(name) || widget === "file") {
      return { type: "text", actionButton: "browse" };
    }
    if (widget === "text") {
      const format = TEXT_FORMATS[name];
      return format ? { type: "text", format } : { type: "text" };
    }
    if (widget === "keyvalue") {
      return { type: "text", paired: true };
    }
    if (widget === "date" || widget === "time" || widget === "datetime") {
      return { type: "date" };
    }
    if (widget === "select" || SELECT_FAMILY.has(name)) {
      return { type: "select" };
    }
    const mapped = WIDGET_TO_EINPUT[widget];
    if (!mapped) return null;
    return { type: mapped };
  }

  // grammar.ts
  var TOKEN_RE = /^input\.([a-z0-9]+)(\.autogenerate)?(?::([\s\S]*))?$/;
  var OUTER_RE = /\{\{\s*([^{}]*?)\s*\}\}/g;
  var CHECK_RE = /^(1|t|true|on|checked|yes|y|0|f|false|off|unchecked|no|n)$/i;
  var CHECK_TRUE_RE = /^(1|t|true|on|checked|yes|y)$/i;
  function splitOpts(s) {
    return s.split(",").map((x) => x.trim()).filter((x) => x !== "");
  }
  function parseToken(inner) {
    inner = String(inner != null ? inner : "").trim();
    const m = inner.match(TOKEN_RE);
    if (!m) return null;
    const name = "input." + m[1];
    const resolved = lookupType(name);
    if (!resolved) return null;
    const def = resolved.row;
    const widget = def[1];
    const auto = !!m[2];
    const raw = (m[3] != null ? m[3] : "").trim();
    let params = raw;
    let defVal = "";
    let optsArr = null;
    let rangeDef = null;
    let checkedDef = null;
    if (widget === "multiselect") {
      if (raw === "") {
        params = def[3] || "";
        optsArr = params ? splitOpts(params) : [];
        defVal = "";
      } else {
        const eq = raw.indexOf("=");
        if (eq >= 0) {
          const ml = raw.slice(0, eq).trim();
          const mr = raw.slice(eq + 1).trim();
          const mlo = ml ? splitOpts(ml) : [];
          const mrd = mr ? splitOpts(mr) : [];
          if (mlo.length) {
            optsArr = mlo;
            defVal = mrd.join(",");
            params = mlo.join(",") + (mrd.length ? "=" + mrd.join(",") : "");
          } else {
            optsArr = splitOpts(raw);
            params = optsArr.join(",");
          }
        } else {
          optsArr = splitOpts(raw);
          params = optsArr.join(",");
        }
      }
    } else if (widget === "select" || widget === "radio" || widget === "buttongroup" || widget === "license") {
      if (raw === "") {
        params = def[3] || "";
        optsArr = params ? splitOpts(params) : [];
      } else {
        const dm = raw.match(/^(.*)=\s*([^=,]+?)\s*$/);
        if (dm && dm[1].trim() !== "" && dm[2].trim() !== "") {
          const left = dm[1].trim();
          const right = dm[2].trim();
          const lopts = splitOpts(left);
          if (lopts.length && right.indexOf(",") < 0) {
            optsArr = lopts;
            defVal = right;
            params = lopts.join(",") + "=" + right;
          } else {
            optsArr = splitOpts(raw);
            params = optsArr.join(",");
          }
        } else {
          optsArr = splitOpts(raw);
          params = optsArr.join(",");
        }
      }
    } else if (widget === "range") {
      params = raw === "" ? def[3] || "" : raw;
      const rm = params.match(
        /(-?\d+(?:\.\d+)?)\s*-\s*(-?\d+(?:\.\d+)?)(?:\s*=\s*(-?\d+(?:\.\d+)?))?/
      );
      if (rm) {
        rangeDef = {
          min: +rm[1],
          max: +rm[2],
          def: rm[3] != null && rm[3] !== "" ? rm[3] : ""
        };
        if (rangeDef.def !== "") defVal = rangeDef.def;
      }
    } else if (widget === "checkbox" || widget === "switch") {
      if (raw === "") {
        params = def[3] || "";
        checkedDef = true;
        if (params !== "") defVal = params;
      } else {
        const cm = raw.match(/^(.*)=\s*([^\s=]+?)\s*$/);
        if (cm && cm[1].trim() !== "" && CHECK_RE.test(cm[2].trim())) {
          params = cm[1].trim() || "--flag";
          checkedDef = CHECK_TRUE_RE.test(cm[2].trim());
          defVal = params;
        } else {
          params = raw;
          checkedDef = true;
          defVal = params;
        }
      }
    } else {
      if (params === "") params = def[3] || "";
      defVal = params;
    }
    let key = name + (auto ? ".autogenerate" : "") + (params !== "" ? ":" + params : "");
    if ((widget === "checkbox" || widget === "switch") && checkedDef === false) {
      key += "=off";
    }
    return {
      key,
      type: widget,
      name,
      params,
      auto,
      default: defVal,
      optionsArr: optsArr,
      rangeDef,
      checkedDef,
      token: "{{" + inner + "}}",
      retired: resolved.retired,
      occ: 1,
      iid: name,
      total: 1
    };
  }
  function ccInstanceKey(base, occ) {
    return occ <= 1 ? base : base + "#" + occ;
  }
  function parseVarInstances(t) {
    const out = [];
    const counts = {};
    const re = new RegExp(OUTER_RE.source, "g");
    let m;
    while (m = re.exec(String(t != null ? t : ""))) {
      let p = null;
      try {
        p = parseToken(m[1]);
      } catch (e) {
        p = null;
      }
      if (!p) continue;
      const occ = (counts[p.key] || 0) + 1;
      counts[p.key] = occ;
      p.occ = occ;
      p.iid = ccInstanceKey(p.key, occ);
      out.push(p);
    }
    const totals = {};
    for (const v of out) totals[v.key] = (totals[v.key] || 0) + 1;
    for (const v of out) v.total = totals[v.key] || 1;
    return out;
  }
  function parseVars(t) {
    const out = [];
    const seen = /* @__PURE__ */ new Set();
    const re = new RegExp(OUTER_RE.source, "g");
    let m;
    while (m = re.exec(String(t != null ? t : ""))) {
      const p = parseToken(m[1]);
      if (p && !seen.has(p.key)) {
        seen.add(p.key);
        out.push(p);
      }
    }
    return out;
  }
  function renderCmd(t, vals, meta) {
    const counts = {};
    return String(t != null ? t : "").replace(
      new RegExp(OUTER_RE.source, "g"),
      (m, inner) => {
        const p = parseToken(inner);
        if (!p) return m;
        const occ = (counts[p.key] || 0) + 1;
        counts[p.key] = occ;
        const k = ccInstanceKey(p.key, occ);
        if (vals) {
          if (k in vals) return vals[k];
          if (p.key in vals) return vals[p.key];
        }
        if (meta) {
          const mk = meta[k];
          const mb = meta[p.key];
          for (const cand of [mk, mb]) {
            if (typeof cand === "string" && cand !== "") return cand;
            if (cand && typeof cand === "object" && typeof cand.example === "string" && cand.example !== "") {
              return cand.example;
            }
          }
        }
        return m;
      }
    );
  }
  function rangeSpec(params) {
    const [boundsPart, stepPart] = String(params != null ? params : "").split(":");
    const bounds = (boundsPart || "").replace(/=\s*-?\d+(?:\.\d+)?\s*$/, "");
    const mm = bounds.trim().match(
      /^(-?\d+(?:\.\d+)?)\s*-\s*(-?\d+(?:\.\d+)?)$/
    );
    let min = mm ? parseFloat(mm[1]) : 0;
    let max = mm ? parseFloat(mm[2]) : 100;
    if (!isFinite(min)) min = 0;
    if (!isFinite(max)) max = 100;
    if (max <= min) max = min + 100;
    let step = stepPart !== void 0 ? parseFloat(stepPart) : 1;
    if (!isFinite(step) || step <= 0) step = 1;
    return { min, max, step };
  }
  function ccUuid() {
    try {
      if (typeof crypto !== "undefined" && typeof crypto.randomUUID === "function") {
        return crypto.randomUUID();
      }
    } catch (e) {
    }
    return "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx".replace(/[xy]/g, (c) => {
      const r = Math.random() * 16 | 0;
      const v = c === "x" ? r : r & 3 | 8;
      return v.toString(16);
    });
  }
  function ccRandStr(n) {
    const ch = "abcdefghijklmnopqrstuvwxyz0123456789";
    let s = "";
    for (let i = 0; i < (n || 8); i++) {
      s += ch[Math.floor(Math.random() * ch.length)];
    }
    return s;
  }
  function ccRandInt(a, b) {
    a = +a;
    b = +b;
    if (!isFinite(a)) a = 0;
    if (!isFinite(b)) b = 100;
    return String(a + Math.floor(Math.random() * (b - a + 1)));
  }
  function ccAutoGenerate(v) {
    const n = v.name;
    const w = v.type;
    if (n === "input.uuid") return ccUuid();
    if (w === "password") return "pw-" + ccRandStr(12);
    if (w === "color") {
      return "#" + ("000000" + Math.floor(Math.random() * 16777215).toString(16)).slice(-6);
    }
    if (w === "date" || n === "input.since") {
      try {
        return (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
      } catch (e) {
        return "2026-09-12";
      }
    }
    if (w === "time") {
      try {
        return (/* @__PURE__ */ new Date()).toISOString().slice(11, 16);
      } catch (e) {
        return "12:30";
      }
    }
    if (w === "datetime") {
      try {
        return (/* @__PURE__ */ new Date()).toISOString().slice(0, 16);
      } catch (e) {
        return "2026-09-12T12:30";
      }
    }
    if (w === "number") {
      if (n === "input.port") return ccRandInt(3e3, 8999);
      if (n === "input.bitrate") return ccRandInt(800, 6e3);
      if (n === "input.fps") return "30";
      if (n === "input.count" || n === "input.quantity" || n === "input.replicas") {
        return ccRandInt(1, 9);
      }
      return ccRandInt(1, 999);
    }
    if (n === "input.email") return "user-" + ccRandStr(5) + "@example.com";
    if (n === "input.url") return "https://example.com/" + ccRandStr(6);
    if (n === "input.ip") {
      return ccRandInt(11, 250) + "." + ccRandInt(0, 250) + "." + ccRandInt(0, 250) + "." + ccRandInt(2, 250);
    }
    if (n === "input.username") return "user-" + ccRandStr(5);
    if (n === "input.hostname" || n === "input.domain") {
      return "host-" + ccRandStr(4) + ".example.com";
    }
    if (w === "text" || w === "file") {
      return n === "input.uuid" ? ccUuid() : ccRandStr(8);
    }
    return null;
  }
  var FILE_EXAMPLES = {
    "input.file": "input.mp4",
    "input.output": "output.mp4",
    "input.subtitle": "subs.srt",
    "input.poster": "poster.jpg",
    "input.thumbnail": "thumb.jpg",
    "input.path": "./src/app.js",
    "input.dir": "./dist",
    "input.folder": "./dist",
    "input.files": "input.mp4 input2.mp4",
    "input.template": "template.hbs",
    "input.config": "config.json",
    "input.logfile": "app.log",
    "input.workdir": "C:\\projects\\app",
    "input.varfile": "vars.tfvars",
    "input.planfile": "plan.out"
  };
  var NUMBER_EXAMPLES = {
    "input.port": "8080",
    "input.bitrate": "2500",
    "input.fps": "30",
    "input.count": "3",
    "input.quantity": "2",
    "input.replicas": "3",
    "input.timeout": "30",
    "input.retries": "3",
    "input.delay": "5",
    "input.cpu": "2",
    "input.speed": "1.5"
  };
  var TEXT_EXAMPLES = {
    "input.text": "hello",
    "input.message": "hello world",
    "input.title": "Release notes",
    "input.description": "Small fix release",
    "input.username": "ada",
    "input.hostname": "web-01",
    "input.domain": "example.com",
    "input.ip": "192.168.1.10",
    "input.email": "you@example.com",
    "input.url": "https://example.com",
    "input.search": "button styles",
    "input.regex": "[a-z]+",
    "input.duration": "00:01:20",
    "input.seek": "00:00:10",
    "input.filename": "app.js",
    "input.glob": "*.js",
    "input.pattern": "*.test.js",
    "input.script": "build",
    "input.package": "opencode",
    "input.version": "1.2.0",
    "input.semver": "1.2.0",
    "input.branch": "main",
    "input.tag": "v1.2.0",
    "input.commit": "a1b2c3d",
    "input.author": "Ada Lovelace",
    "input.repos": "open-design/app",
    "input.org": "acme",
    "input.cimage": "node:20",
    "input.containername": "web",
    "input.mount": "./data:/data",
    "input.network": "app-net",
    "input.memory": "512m",
    "input.entrypoint": "npm start",
    "input.header": "Authorization: Bearer TOKEN",
    "input.query": "q=hello&page=2",
    "input.cidr": "10.0.0.0/24",
    "input.sql": "SELECT * FROM users",
    "input.mac": "02:42:ac:11:00:02",
    "input.database": "appdb",
    "input.table": "users",
    "input.dbuser": "admin",
    "input.cron": "0 * * * *",
    "input.namespace": "default",
    "input.deployment": "web",
    "input.service": "api",
    "input.pod": "web-0",
    "input.context": "prod",
    "input.region": "us-east-1",
    "input.profile": "default",
    "input.bucket": "assets",
    "input.bucketkey": "video/input.mp4",
    "input.workspace": "main",
    "input.hexcolor": "#1677ff",
    "input.bgcolor": "#ffffff",
    "input.fgcolor": "#1f2328",
    "input.colorname": "steelblue",
    "input.flag": "--flag",
    "input.confirm": "--yes",
    "input.verbose": "--verbose",
    "input.quiet": "--quiet",
    "input.force": "--force",
    "input.dryrun": "--dry-run"
  };
  function exampleFor(v, meta) {
    if (meta && meta.example !== void 0 && meta.example !== null && meta.example !== "") {
      return String(meta.example);
    }
    const n = v.name;
    const w = v.type;
    const p = v.params || "";
    if (n === "input.uuid" && v.default) return v.default;
    if (n === "input.uuid" && !v.auto) return "123e4567-e89b-12d3-a456-426614174000";
    if (w === "multiselect") {
      const mo = v.optionsArr && v.optionsArr.length ? v.optionsArr : p.split(",").map((s) => s.trim()).filter((s) => s !== "");
      if (!mo.length) return "opt";
      const md = String(v.default || "").split(",").map((s) => s.trim()).filter(
        (s) => mo.indexOf(s) >= 0
      );
      if (md.length) return md.join(",");
      return mo[0];
    }
    if (w === "select" || w === "radio" || w === "buttongroup" || w === "license") {
      const o = v.optionsArr && v.optionsArr.length ? v.optionsArr : p.split(",").map((s) => s.trim()).filter((s) => s !== "");
      if (!o.length) return "opt";
      if (v.default && o.indexOf(v.default) >= 0) return v.default;
      return o[0];
    }
    if (w === "range") {
      if (v.rangeDef && v.rangeDef.def !== "" && isFinite(+v.rangeDef.def)) {
        return String(+v.rangeDef.def);
      }
      if (v.default !== "" && isFinite(+v.default)) return String(+v.default);
      const rm = p.match(/(-?\d+)\s*-\s*(-?\d+)/);
      if (rm) {
        const a = +rm[1];
        const b = +rm[2];
        return String(Math.round((a + b) / 2));
      }
      return "50";
    }
    if (w === "checkbox" || w === "switch") {
      if (v.checkedDef === false) return "";
      return p || "--flag";
    }
    if (v.default !== "") return v.default;
    if (v.auto) {
      try {
        const ag = ccAutoGenerate(v);
        if (ag != null && ag !== "") return ag;
      } catch (e) {
      }
    }
    if (w === "color") {
      if (n === "input.colorname") return "steelblue";
      return "#1677ff";
    }
    if (w === "date" || n === "input.since") return "2026-09-12";
    if (w === "password") return "s3cr3t";
    if (w === "time") return "12:30";
    if (w === "datetime") return "2026-09-12T12:30";
    if (w === "textarea") return "hello world";
    if (w === "keyvalue") {
      const kv = String(p || "");
      if (kv.indexOf("=") >= 0) return kv;
      return "KEY=value";
    }
    if (w === "file") return FILE_EXAMPLES[n] || "input.mp4";
    if (w === "number") {
      if (NUMBER_EXAMPLES[n]) return NUMBER_EXAMPLES[n];
      return "42";
    }
    if (TEXT_EXAMPLES[n]) return TEXT_EXAMPLES[n];
    return "hello";
  }

  // render.ts
  var OPTION_TAG = {
    select: "e-select-option",
    radio: "e-radio-option",
    multiselect: "e-checkbox-option"
  };
  function optionTag(widget) {
    if (widget === "radio" || widget === "buttongroup") return "e-radio-option";
    if (widget === "multiselect") return "e-checkbox-option";
    return OPTION_TAG["select"];
  }
  function metaForOption(meta, value) {
    var _a, _b;
    const opts = meta == null ? void 0 : meta.options;
    const found = Array.isArray(opts) ? opts.find((o) => o.value === value) : opts && typeof opts === "object" ? opts[value] : void 0;
    const label = ((_a = found == null ? void 0 : found.label) == null ? void 0 : _a.trim()) || value;
    const description = ((_b = found == null ? void 0 : found.description) == null ? void 0 : _b.trim()) || void 0;
    return description ? { label, description } : { label };
  }
  function renderToken(token, value = "", meta) {
    var _a;
    const mapping = widgetToEInput({ name: token.name, type: token.type });
    if (!mapping) return null;
    const out = {
      tag: "e-input",
      type: mapping.type,
      name: token.iid,
      value: value != null ? value : "",
      options: []
    };
    if (mapping.format) out.format = mapping.format;
    if (mapping.actionButton) out.actionButton = mapping.actionButton;
    if (mapping.paired) out.paired = true;
    if (meta == null ? void 0 : meta.label) out.label = meta.label;
    else out.label = token.name;
    if (meta == null ? void 0 : meta.description) out.description = meta.description;
    if ((meta == null ? void 0 : meta.example) !== void 0 && meta.example !== "") {
      out.placeholder = meta.example;
    }
    if (token.type === "range") {
      const rs = rangeSpec(token.params || "");
      out.min = rs.min;
      out.max = rs.max;
      out.step = rs.step;
    }
    if (token.type === "checkbox" || token.type === "switch") {
      out.checked = token.checkedDef !== false;
      if (!out.value) out.value = token.checkedDef === false ? "" : token.params || "--flag";
      return out;
    }
    const widget = token.type;
    if (widget === "select" || widget === "radio" || widget === "buttongroup" || widget === "multiselect" || widget === "license") {
      const opts = (_a = token.optionsArr) != null ? _a : [];
      const tag = optionTag(widget);
      out.options = opts.map((v) => {
        const m = metaForOption(meta, v);
        return m.description ? { tag, value: v, label: m.label, description: m.description } : { tag, value: v, label: m.label };
      });
    }
    return out;
  }
  return __toCommonJS(browser_exports);
})();

window.CCGrammar = Object.assign({ GRAMMAR_HASH: "2bfc84d97297ce7c27d320a17ac9aa8d20c6142d7fd68d4ba5ab3fc4d6ef29b4" }, window.CCGrammarSrc);
